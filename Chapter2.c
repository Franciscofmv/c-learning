#include <stdio.h>
#include <stdlib.h>

int main(){
    // int hour, minute, second;
    // hour = 1;
    // minute = 2;
    // second = 4;

    // printf("The time is %i:%i:%i\n", hour, minute, second);
    // printf("%c\n", '5');
    char letter = 'd';
    printf("%c", letter*9);
    return(EXIT_SUCCESS);
}